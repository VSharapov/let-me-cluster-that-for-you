# kubernetes cluster

This is copypasta from [the official Azure aks TF module](https://github.com/Azure/terraform-azurerm-aks/tree/master/modules/kubernetes-cluster)

Probably useless since the one with the VPC is preferred
